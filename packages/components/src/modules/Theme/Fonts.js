import { css } from 'styled-components'

const light = css`
  font-weight: 300;
`

const regular = css`
  font-weight: 400;
`

const semiBold = css`
  font-weight: 600;
`

const bold = css`
  font-weight: 700;
`

export const FontStyles = {
  light,
  regular,
  semiBold,
  bold,
}
